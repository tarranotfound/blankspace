# Maintenance Notes

Keep configuration files organized by component and remove temporary experiments once a direction is finalized.

Small, focused changes make the repository easier to review and maintain.
